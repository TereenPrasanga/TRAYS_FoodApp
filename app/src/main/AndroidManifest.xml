<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.trays_foodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".shop_delete_list">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterdUserList"></activity>
        <activity android:name=".SignIn" />
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".shopView_list" />
        <activity android:name=".shop_register"></activity>
        <activity android:name=".user_registration"></activity>
    </application>

</manifest>